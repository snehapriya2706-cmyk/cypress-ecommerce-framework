Test Scenarios for Audience Validator:
  
   Verify if empty file is uploaded in the system
   Verify if excel format is followed.
   Verify if the user can upload a list of NPI's in the system
   Verify that the NPI number should be First column
   Verify error message when NPI number not entered and excel uploaded.
   Verify if system allows to select filters before uploading file.5
   Verify that the filters are provided to select client, brand and RFP.
   Check if system is able to match NPI's which already exist in system
   Verify if data can be updated and uploaded to NPI's who already present in system
   Verify if the old data gets removed when new data is updated.
   Verify system have the ability to group results by different fields.
   
   
   
   